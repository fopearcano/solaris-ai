# Decision log — sub-modules round (before the Console)

All questions from the "questions before the Console" round were answered
**"all defaults, proceed"**. Recorded here for the record; this round implements
them.

## Actions (Group A)
1. Reborn = **in-process reset** to 0 (rebuild Conscience on a fresh Bus +
   Lifecycle; the browser stays connected, experienced as a blackout + new life).
2. Sleep triggers = **operator + auto** on sustained low arousal.
3. oMBA / oTD = **run together** under Sleep; each separately observable.
4. oMBA = Synthesis pruning + Habit consolidation + energy (arousal) recovery;
   oTD = Memory replay re-fed as internal `oneiric` Stimuli + the normal
   meaning/anticipation pipeline (dreaming).
5. Re-Gain = **auto after a sleep duration, or manual**.
6. Activate trigger = **a `threat` modality auto-activates; operator can force**.
7. Activate = raise arousal, lower I/O threshold, prioritise reactive, **suspend
   non-vital Limitations**.
8. Activate **decays** unless re-triggered (adrenaline).

## SIC → RO (Group B)
9. SIC = **seed table (operator) + Habit reweight**.
10. "Physical" RO = **real substrate act now** (a tiny bounded compute that shows
    up in the resource stats — the body acting on its own substrate).
11. Relevance = **Logos-fracture × SIC-determinacy** (entropy shift).

## Negation / NO / e.Link (Group C)
12. NO emitted by the **operator** for now (sibling/agent reserved for Contact).
13. e.Link **grown from sustained interaction**, with operator override.
14. **Only from an Other** for v1 (no self-negation yet).
15. A Limitation = a forbidden `meaning` (filters RO) **+** an Inner-MAP boundary.
16. Limitations: **slow decay + suspend (Activate) + wipe (Reborn)**.

## Gating decisions
26. New sub-modules live in **core-Conscience** (the NN inherits them).
27. Implement **Actions + SIC→RO now**; NO/e.Link landed too (Q12–14 settled by
    defaults).

## Console (Group D) — deferred
The Console is added in a later round. Defaults recorded: control everything
except save/load (forbidden on principle — no resurrection, Q21); read+write;
expose all parameters; per-module pause/resume; verb-first CLI syntax; coexist
with existing buttons; log commands to the biography. The command surface built
this round (sleep/wake/activate/reborn/negate + existing stimulate/react/die)
is what the Console will wrap.
